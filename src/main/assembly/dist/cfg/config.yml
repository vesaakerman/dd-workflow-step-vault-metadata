#
# Module configuration file
#
# Please, modify the configuration below to match your environment. Also, delete and/or modify
# this description.

server:
  type: default
  maxThreads: 1024


taskExecutorThreadPool:
  minThreads: 1
  maxThreads: 1
  maxIdleSeconds: 60
